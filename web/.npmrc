# Supply-chain hardening (SPEC §11 / JS dependency policy).
# Block install-time code execution (the May-2026 worm runs via lifecycle scripts).
ignore-scripts=true
# Never write version ranges; pins stay exact.
save-exact=true
fund=false
audit-level=moderate
